<aside id="lateral">

    <!-- Solo se muestra el carrito si hay algo en el-->
    {% set carrito= app.session.get('carrito') %}
    {% if( carrito|length >0 ) %}
        {% set total=0 %}
        {% for elemento in carrito %}
            {% set total = total+(elemento.unidades*elemento.precio) %}
        {% endfor %}
        <div id="carrito" class="block_aside">
            <h3>Mi carrito</h3>
            <ul>
                <li><a href="{{ path('carritoindex') }}">Productos ({{ carrito|length }})</a></li>
                <li><a href="{{ path('carritoindex') }}">Total: ${{ total }} </a></li>
                <li><a href="{{ path('carritoindex') }}">Ver el carrito</a></li>
            </ul>
        </div>
    {% endif %}



    {#
    <div id="login" class="block_aside">
        @if(session('validado')=="ok")
            <strong class="alert_green">¡ Bienvenido {{ (session('usuario'))->nombre }} !</strong>
            <hr/><br/>
        @elseif(session('validado')=="noOk")
            <strong class="alert_red">Ingreso Fallido</strong>
        @elseif(session('validado')=="chau")
            <strong class="alert_red">Chau {{ session('nombre_usuario') }}, gracias por usar el sitio</strong>
            <hr/><br/>
        @endif
        @if(session('identity'))
            <h3>{{(session('usuario'))->nombre}} {{(session('usuario'))->apellidos}}</h3>
            <ul>
            @if((session('usuario'))->rol=="admin")
                <li><a href="{{ route('categoriaindex') }}">Gestionar categorias</a></li>
                <li><a href="{{ route('productogestion') }}">Gestionar productos</a></li>
                <li><a href="{{ route('pedidogestion') }}">Gestionar pedidos</a></li>
            @endif
            <li><a href="{{ route('pedido_mispedidos') }}">Mis pedidos</a></li>
             <li><a href="{{ route('logout') }}">Cerrar sesion</a></li>
            </ul>
        @else
            <h3>Entrar a la web</h3>
            <form action="{{ route('usuariologin') }}" method="post">
                @csrf
                <label for="email">Email</label>
                <input type="email" name="email" />
                <label for="password">Contraseña</label>
                <input type="password" name="password" />
                <input type="submit" value="Enviar" />
            </form>
            <ul>
                <li><a href="{{ route('usuarioregistro') }}">Registrate aqui</a></li>
            </ul>
        @endif
    </div>

    #}

</aside>