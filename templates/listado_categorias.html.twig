@extends('base.layout')

@section('contenidocentral')
    <!-- CONTENIDO CENTRAL -->
    <div id="central"><h1>Gestionar categorias</h1>

        @if(session('altacategoria'))
            @if(session('altacategoria')==1)
                <strong class="alert_green">Categoria Creada Correctamente</strong>
            @else
                <strong class="alert_red">Categoria no creada</strong>
            @endif
        @endif

        <a href="{{ route('categoriacrear') }}" class="button button-small">
            Crear categoria
        </a>
        <table>
            <tr>
                <th>ID</th>
                <th>NOMBRE</th>
            </tr>
            @foreach($categorias as $categoria)
                <tr>
                    <td>{{$categoria->id}}</td>
                    <td>{{$categoria->nombre}}</td>
                </tr>
            @endforeach
        </table>
    </div>
    </div>
@endsection