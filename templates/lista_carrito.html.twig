@extends('base.layout')

@section('contenidocentral')
    <div id="central"><h1>Carrito de la compra</h1>
        @if(count($carrito)>0)
            @if(session('errorenstock'))
                <strong class="alert_green">No hay stock de algunos productos, revise</strong>
            @endif
            <table>
                <tr>
                    <th>Imagen</th>
                    <th>Nombre</th>
                    <th>Precio</th>
                    <th>Unidades</th>
                    <th>Eliminar</th>
                </tr>
                <?php $total=0; ?>
                @foreach($carrito as $indice => $elemento)
                    <tr>
                        <td>
                            @if($elemento['imagen'] != null)
                                <img src="{{route('productoimagen',['nombre_imagen'=>$elemento['imagen']])}}" class="img_carrito" />
                            @else
                                <img src="{{route('productoimagen',['nombre_imagen'=>"camiseta.png"])}}" class="img_carrito" />
                            @endif
                        </td>
                        <td>
                            <a href="{{ route('productover',['id'=>$elemento['id']])}}"> {{$elemento['nombre']}}</a>
                        </td>
                        <td>
                            {{$elemento['precio']}}
                        </td>
                        <td>
                            {{ $elemento['unidades'] }}
                            <div class="updown-unidades">
                                <a href="{{ route('carritoup',['indice_en_carrito'=>$indice]) }}" class="button">+</a>
                                <a href="{{ route('carritodown',['indice_en_carrito'=>$indice]) }}" class="button">-</a>
                            </div>
                        </td>
                        <td>
                            <a href="{{ route('carritodelete',['indice_en_carrito'=>$indice]) }}" class="button button-carrito button-red">Quitar producto</a>
                        </td>
                    </tr>
                    <?php $total+=$elemento['unidades']*$elemento['precio']; ?>
                @endforeach
            </table>
            <br/>
            <div class="delete-carrito">
                <a href="{{ route('carritodeleteall') }}" class="button button-delete button-red">Vaciar carrito</a>
            </div>
            <div class="total-carrito">
                <h3>Precio total: {{$total}}$</h3>
                <a href="{{ route('pedidohacer') }}" class="button button-pedido">Hacer pedido</a>
            </div>
        @else
            <p>El carrito está vacio, añade algun producto</p>
        @endif
    </div>
@endsection